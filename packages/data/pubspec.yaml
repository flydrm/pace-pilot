name: data
description: "Pace Pilot data layer (database, repositories, backup/export)."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.0.0
  drift_flutter: ^0.2.0
  flutter_local_notifications: ^19.5.0
  flutter_secure_storage: ^10.0.0
  path: ^1.9.1
  path_provider: ^2.1.5
  sqlite3_flutter_libs: ^0.5.0
  timezone: ^0.10.1

  domain:
    path: ../domain
  archive: ^4.0.7
  cryptography: ^2.9.0

dev_dependencies:
  build_runner: ^2.4.12
  drift_dev: ^2.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
